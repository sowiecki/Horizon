<!DOCTYPE html>
<html>
  <head>
    <title>Horizon</title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application', 'ui' %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Lato&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <header>
      <div class='header-content-wrapper'>
        <section class='logo'>
          <%= render 'layouts/header' %>
        </section>
        <nav>
          <% if logged_in? %>
            <%= render 'layouts/logged_in_section' %>
          <% else %>
            <%= render 'layouts/logged_out_section' %>
          <% end %>
        </nav>
      </div>
    </header>
      <%= yield %>
    <footer>
      <%= link_to "#{image_tag('github_icon.png', height: '22px')}".html_safe, "https://github.com/Nase00/Horizon" %>
    </footer>
  </body>
</html>
